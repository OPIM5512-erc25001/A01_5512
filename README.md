## 🔧 How to Run the Code
Follow the steps below to generate the boxplot:
### 1. Clone the repository
```bash
git clone https://github.com/OPIM5512-erc25001/A01_5512.git
cd A01_5512
### 2.  if requirement is present, install required python packages
pip install -r requirements.text
### 3. Run the boxplot script
python src/boxplot.py
### 4. Expected output
Running the script will:
Load the data (as defined in boxplot.py)
Generate a boxplot
Save the image to:
figs/boxplot.png
after running, you should see boxplot.png inside figs/folder.
